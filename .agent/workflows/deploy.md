---
description: Deploy changes to Railway
---

1. Check git status
   `git status`

2. Add all changes
   `git add .`

3. Commit changes (if any)
   `git commit -m "Auto-deploy: Update application code"`

4. Push to Railway
   // turbo
   `git push railway main`
